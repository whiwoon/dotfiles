{{- if eq .chezmoi.os "windows" }}
# Unix-like shell files and helper scripts. Windows native uses PowerShell, not zsh.
.chezmoiscripts/**
.chezmoiscripts/00-backup-before-apply.sh
.zshenv
.zshrc
.config/antidote/**
.termux/**
.local/bin/**
{{- else }}
# Windows PowerShell profile; only apply on Windows hosts.
Documents/PowerShell/
# Do not remove legacy Windows WezTerm config files on Unix-like hosts.
.wezterm.lua
{{- end }}
