# Managed by chezmoi. Keep this minimal: it runs for every zsh invocation.
# wing now uses the system zsh package as the login shell. Keep this file empty
# unless a setting truly must apply to every zsh invocation.
